v1.3.9+4
+ (plus) [number] versions are intermediate/rolling versions where new features and changes are introduced

added:
- added new threadpool for performance improvement, which can be enable by passing "THREADPOOL" define ( -DTHREADPOOL )

fixes:
- fixed version string CMakeLists.txt which was causing setup failure

updates:
- updated CLIENT_MAX_QUEUE_SIZE to 512
- updated http::Server to accept an optional argument for number of worker threads

known issue:
- library when compiled with clang causes linking issue (macos only)
- tests might fail due to mismatch of order of keys, because of std::unordered_map
